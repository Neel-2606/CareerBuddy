import React from 'react';

function ResumeBuilder() {
  return (
    <section id="resume" className="section resume-builder">
      <h2>Resume Builder</h2>
      <p>
        Build a modern, professional resume in minutes. Customize sections, choose templates, and export as PDF.
        Accessible only after login.
      </p>
      <button>Start Building (Login Required)</button>
    </section>
  );
}

export default ResumeBuilder;
